"use strict";

const states = {
    ok: 'ok',
    warning: 'warning',
    buggered: 'buggered',
    failure: 'failure'
};

function analyse(journey) {
    
}

module.exports = {
    analyse,
    states
}